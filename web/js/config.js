/**
 * 词云 Kiosk 配置
 * GitHub Pages 等纯静态部署时，可将 segmentApi 设为已部署的 Python 分词服务地址。
 * 例: "https://your-host.example.com/api/segment"
 */
window.CIYUN_CONFIG = window.CIYUN_CONFIG || {
  segmentApi: "",
};
